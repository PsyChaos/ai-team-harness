#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck disable=SC1091
source "$SCRIPT_DIR/lib.sh"
set -a
load_runtime
load_secrets
set +a
exec python3 "$SCRIPT_DIR/../coordinator/broker.py" "$@"
