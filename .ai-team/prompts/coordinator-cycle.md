Deprecated: model-driven coordinator cycles are disabled.

The coordinator is deterministic code in `.ai-team/coordinator/broker.py`.
This prompt must never be sent to a provider with tools or credentials.

No model output from this file is accepted as an action, command, path, status,
issue/PR mutation, or authorization decision.
